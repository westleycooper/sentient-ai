[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sentient-domain"
version = "0.1.0"
requires-python = ">=3.12"
license = "Apache-2.0"
dependencies = ["pydantic>=2"]

[tool.hatch.build.targets.wheel]
packages = ["src/sentient_domain"]
